<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="reservation">
	
	<insert id="save" parameterType="dto.ReservationDTO">
	    insert into reservation
	    values(res_seq.nextval, #{res_date}, #{hospital_hosnum,jdbcType=INTEGER}, #{member_id,jdbcType=VARCHAR}, #{petpet})
	</insert>
	
 	<select id="r_search" parameterType="String" resultType="dto.ReservationDTO">
		select res_num, res_date, hospital_hosnum, member_id, petpet
		from reservation
		where member_id=#{member_id}
		order by res_date DESC
	</select> 
	
	<delete id="r_delete" parameterType="int">
		delete 
		from reservation
		where res_num = #{res_num}
	</delete>


</mapper>



















 
 
 
 
 
 
