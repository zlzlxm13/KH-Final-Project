<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="reservation">
	
	<insert id="save" parameterType="dto.ReservationDTO">
	    insert into reservation(res_num, res_date, hospital_hosnum, member_id)
	    values(#{res_num,jdbcType=INTEGER}, #{res_date,jdbcType=DATE}, #{hospital_hosnum,jdbcType=INTEGER}, #{member_id},jdbcType=VARCHAR)
	</insert>
	

</mapper>


















 
 
 
 
 
 
